aiohttp
configparser
future

# Additional Packages for export Functionality
pandas>=2.0.0

# Extra libraries are installed when needed using pipmaster

pipmaster
pydantic
python-dotenv

# Unicode Collation Algorithm for proper Chinese sorting
pyuca

setuptools
tenacity

# LLM packages
tiktoken
xlsxwriter>=3.1.0


uvicorn[standard]

    fastapi
    python-multipart # Often needed by FastAPI for form data/uploads
    python-dotenv # Already installing, but good to ensure
    # Add any other direct imports from lightrag/api/lightrag_server.py if they cause errors later